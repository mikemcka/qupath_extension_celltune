---
phase: 11-project-summary-cohort-outlier-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/main/java/qupath/ext/celltune/CellTuneExtension.java
  - src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java
  - src/main/java/qupath/ext/celltune/io/ProjectSummaryCsvExporter.java
  - src/test/java/qupath/ext/celltune/io/ProjectSummaryCsvExporterTest.java
autonomous: true
requirements:
  - COH-03
  - COH-04
must_haves:
  truths:
    - "Users can rank and filter images by anomaly score and flagged status in the project summary dialog"
    - "Users can target rare classes (such as NK or Treg) to focus review on enriched images"
    - "CSV export includes anomaly and enrichment fields for external audit"
  artifacts:
    - path: "src/main/java/qupath/ext/celltune/CellTuneExtension.java"
      provides: "Integration of cohort analyzer output into summary row construction"
      contains: "analyzer invocation and enriched row payload"
    - path: "src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java"
      provides: "UI columns, filters, and details for anomaly-aware project review"
      contains: "anomaly score, flagged filter, target-class filter, reason text"
    - path: "src/main/java/qupath/ext/celltune/io/ProjectSummaryCsvExporter.java"
      provides: "Centralized CSV schema writer for summary exports"
      contains: "new analytics columns and escaping rules"
    - path: "src/test/java/qupath/ext/celltune/io/ProjectSummaryCsvExporterTest.java"
      provides: "Automated regression checks for summary CSV schema"
      contains: "header fields and escaped row output assertions"
  key_links:
    - from: "src/main/java/qupath/ext/celltune/CellTuneExtension.java"
      to: "src/main/java/qupath/ext/celltune/model/CohortAnomalyAnalyzer.java"
      via: "summary row assembly uses analyzer report by image name"
      pattern: "CohortAnomalyAnalyzer"
    - from: "src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java"
      to: "src/main/java/qupath/ext/celltune/io/ProjectSummaryCsvExporter.java"
      via: "export button delegates to shared CSV writer"
      pattern: "ProjectSummaryCsvExporter"
    - from: "src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java"
      to: "src/main/java/qupath/ext/celltune/model/CohortAnomalyReport.java"
      via: "row model exposes anomaly/rare-enrichment fields from report"
      pattern: "anomalyScore|flagReasons|rare"
---

<objective>
Integrate cohort anomaly insights into the project summary experience so users can find rare-cell enrichments and suspicious images quickly.

Purpose: Convert backend anomaly scoring into actionable ranking/filter/export workflows for project-level review.
Output: Summary dialog enhancements, export schema updates, and regression tests for CSV output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-project-summary-cohort-outlier-analytics/11-01-PLAN.md
@src/main/java/qupath/ext/celltune/CellTuneExtension.java
@src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java

<interfaces>
From src/main/java/qupath/ext/celltune/model/CohortAnomalyReport.java (from plan 11-01):
```java
public final class CohortAnomalyReport {
  public List<ImageAnomaly> images();

  public record ImageAnomaly(
      String imageName,
      double anomalyScore,
      boolean flagged,
      List<String> flagReasons,
      Map<String, ClassEnrichment> enrichmentByClass) {}

  public record ClassEnrichment(
      String className,
      long count,
      double baselineFraction,
      double imageFraction,
      double enrichmentFold,
      boolean rareCandidate,
      boolean highlighted) {}
}
```
Use these contracts directly; do not duplicate analytics logic in UI.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate analyzer report into project summary row assembly</name>
  <files>src/main/java/qupath/ext/celltune/CellTuneExtension.java</files>
  <behavior>
    - Test 1: For images with predictions, summary row includes anomaly score and reason fields when report marks the image flagged.
    - Test 2: For images without predictions, summary row keeps no-prediction messaging and zeroed analytics values.
  </behavior>
  <action>Refactor summary data assembly to first collect per-image predicted counts, class counts, and disagreement rates, then call `CohortAnomalyAnalyzer.analyze(...)`. Join analyzer results by image name when creating summary rows and include anomaly score, flagged state, reasons, and rare-enrichment text fields in row payloads. Preserve existing fallback behavior for missing predictions and maintain current image/open-image workflow.</action>
  <verify>
    <automated>grep -nE "CohortAnomalyAnalyzer|anomalyScore|flagged|flagReasons|buildPredictionSummaryRow" src/main/java/qupath/ext/celltune/CellTuneExtension.java</automated>
  </verify>
  <done>Summary rows are analyzer-backed and include all anomaly metadata required by UI/filter/export layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add anomaly-aware columns, filters, and detail panes in summary UI</name>
  <files>src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java</files>
  <behavior>
    - Test 1: Flagged-only filter hides unflagged rows and updates table count summary.
    - Test 2: Target-class filter surfaces rows with highlighted enrichment for selected class.
    - Test 3: Default sort orders by anomaly score descending, then disagreements descending.
  </behavior>
  <action>Extend `Row` to hold anomaly score, flagged marker, reason text, and rare-enrichment summary text. Add table columns for anomaly and flagged state. Add controls for `Flagged only`, `Target class`, and threshold preset selector (strict/balanced/sensitive) applied client-side to row visibility. Update detail text area to include enrichment lines for highlighted rare classes and reason tags for outlier flags.</action>
  <verify>
    <automated>grep -nE "Anomaly|Flagged|Target class|strict|balanced|sensitive|Filtered" src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java</automated>
  </verify>
  <done>Users can sort/filter for rare or suspicious images directly in the summary dialog without external processing.</done>
</task>

<task type="auto">
  <name>Task 3: Centralize CSV export schema and add regression tests</name>
  <files>src/main/java/qupath/ext/celltune/io/ProjectSummaryCsvExporter.java, src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java, src/test/java/qupath/ext/celltune/io/ProjectSummaryCsvExporterTest.java</files>
  <action>Create `ProjectSummaryCsvExporter` as a pure-Java writer that accepts summary rows and returns CSV text with columns: image, predicted counts, agreement/disagreement, agreement percent, anomaly score, flagged, flag reasons, rare enrichment, class counts. Wire the summary export button to this utility so logic is testable without JavaFX. Add regression tests for header schema, row ordering stability, and CSV escaping of comma/quote-rich fields.</action>
  <verify>
    <automated>./gradlew test --tests "qupath.ext.celltune.io.ProjectSummaryCsvExporterTest"</automated>
  </verify>
  <done>CSV export carries anomaly analytics fields and automated tests prevent schema/escaping regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyzer report -> summary UI | Untrusted or malformed report values can mislead filtering and ranking behavior |
| UI filters -> user review decisions | Incorrect filter logic can hide high-risk images or rare-type candidates |
| Export serialization -> downstream analysis | Missing/incorrect CSV fields can corrupt audit and external triage workflows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | T | Summary row integration in CellTuneExtension | mitigate | Join analyzer results by image name with null-safe fallbacks and preserve no-prediction defaults |
| T-11-06 | D | UI filtering logic in ProjectPredictionSummaryView | mitigate | Keep filter operations deterministic, show filtered counts, and avoid hidden implicit thresholds |
| T-11-07 | I | CSV export schema | mitigate | Centralize writer in ProjectSummaryCsvExporter and lock schema with automated tests |
| T-11-08 | R | Analytics-to-UI traceability | mitigate | Include explicit reason tags and rare-enrichment text in both table details and CSV |
</threat_model>

<verification>
- `grep -nE "CohortAnomalyAnalyzer|anomalyScore|flagged|flagReasons|buildPredictionSummaryRow" src/main/java/qupath/ext/celltune/CellTuneExtension.java`
- `grep -nE "Anomaly|Flagged|Target class|strict|balanced|sensitive|Filtered" src/main/java/qupath/ext/celltune/ui/ProjectPredictionSummaryView.java`
- `./gradlew test --tests "qupath.ext.celltune.io.ProjectSummaryCsvExporterTest"`
</verification>

<success_criteria>
- Project summary ranks and filters images using anomaly analytics from plan 11-01.
- Rare-class targeted review is available in UI controls for fast NK/Treg triage workflows.
- CSV exports include anomaly analytics columns with regression-tested schema and escaping.
</success_criteria>

<output>
After completion, create `.planning/phases/11-project-summary-cohort-outlier-analytics/11-02-SUMMARY.md`
</output>
