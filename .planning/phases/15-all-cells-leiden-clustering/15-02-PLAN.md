---
phase: 15-all-cells-leiden-clustering
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/main/java/qupath/ext/celltune/model/LeidenModel.java
  - src/main/java/qupath/ext/celltune/model/AnnRecallException.java
  - src/test/java/qupath/ext/celltune/model/LeidenModelTest.java
autonomous: true
requirements: [LEI-07, LEI-10]
user_setup: []

must_haves:
  truths:
    - "Leiden clustering can be built from an HNSW-derived kNN graph instead of brute-force featureKnn, in both single-image and cohort scope"
    - "Each ANN Leiden run measures recall on a capped subsample against exact featureKnn and only proceeds when recall ≥ 0.95"
    - "When recall stays < 0.95 after ef escalation, the run aborts by throwing AnnRecallException and produces no labels"
    - "HNSW-routed Leiden labels agree with exact-kNN Leiden within an ARI tolerance on synthetic data"
  artifacts:
    - path: "src/main/java/qupath/ext/celltune/model/AnnRecallException.java"
      provides: "Unchecked exception for the recall-gate abort (no labels written)"
      contains: "extends RuntimeException"
    - path: "src/main/java/qupath/ext/celltune/model/LeidenModel.java"
      provides: "clusterViaAnn entry + recall-gate helper (exact query-vs-reference, meanRecall, ef escalation)"
      contains: "clusterViaAnn"
    - path: "src/test/java/qupath/ext/celltune/model/LeidenModelTest.java"
      provides: "recall-gate pass/escalate/abort + HNSW-vs-exact ARI tests"
  key_links:
    - from: "LeidenModel.clusterViaAnn"
      to: "HnswKnnIndex.knn"
      via: "ANN kNN graph build replacing featureKnn"
      pattern: "HnswKnnIndex"
    - from: "recall gate"
      to: "AnnRecallException"
      via: "throw on recall < 0.95 after escalation cap"
      pattern: "throw new AnnRecallException"
    - from: "clusterViaAnn"
      to: "buildJaccardWeightedNetwork"
      via: "unchanged downstream SNN/Jaccard + CWTS pipeline"
      pattern: "buildJaccardWeightedNetwork"
---

<objective>
Route Leiden's kNN graph construction through the `HnswKnnIndex` from Plan 01, guarded by the runtime recall gate (measure vs exact `featureKnn` on a capped subsample; escalate `ef` geometrically; abort with `AnnRecallException` if still < 95%). The downstream SNN/Jaccard weighting + CWTS Leiden pipeline is unchanged (correctness-first: still uses the existing boxed `buildJaccardWeightedNetwork`; Plan 03 swaps in the primitive-array rewrite for scale).

Purpose: Deliver LEI-07 — HNSW everywhere, recall-validated and enforced (D-07/D-08/D-09).
Output: `AnnRecallException`; a `clusterViaAnn` entry on `LeidenModel`; a recall-gate helper (exact query-vs-reference + mean recall + ef escalation); tests for the pass/escalate/abort paths and HNSW-vs-exact Leiden ARI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-all-cells-leiden-clustering/15-SPEC.md
@.planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md
@.planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md
@.planning/phases/15-all-cells-leiden-clustering/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (HnswKnnIndex.java): -->
  static int[][] HnswKnnIndex.knn(double[][] rows, int k, long seed, boolean reproducible);
  // plus an instance/setEf form if Plan 01 exposed one for query-time ef escalation.

<!-- From LeidenModel.java (existing, reuse as-is): -->
  public static int[][] featureKnn(double[][] rows, int k);          // EXACT baseline for the recall gate
  public static LeidenResult cluster(double[][] rows, int k, double resolution, int randomStarts, long seed);
  // buildJaccardWeightedNetwork(int n, int[][] neighbors) is private — clusterViaAnn feeds it the SAME int[][] shape.
  // nearestReferenceIndices(double[] q, double[][] reference, int keep) (private, lines 383-417) is the
  //   "small query set vs large reference, bounded-heap, no boxing" shape to ADAPT for the exact recall reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnnRecallException + recall-gate helper</name>
  <files>src/main/java/qupath/ext/celltune/model/AnnRecallException.java, src/main/java/qupath/ext/celltune/model/LeidenModel.java, src/test/java/qupath/ext/celltune/model/LeidenModelTest.java</files>
  <read_first>
    - src/main/java/qupath/ext/celltune/model/LeidenModel.java (lines 326-417 `transferLabels`/`nearestReferenceIndices` — adapt into an indices-only exact helper; lines 79-172 bounded-heap primitives to reuse)
    - src/test/java/qupath/ext/celltune/model/LeidenModelTest.java (lines 213-231 `assertDoesNotThrow`/degenerate conventions; add `assertThrows` import)
    - .planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md ("New exception type needed" — new exception, no existing analog, ~L147-148; "Small query set vs large reference set, brute force, parallel" reuse-don't-rewrite pattern, ~L98-117)
    - .planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md ("Architecture Patterns: Pattern 1 Recall Gate", "Common Pitfalls: Pitfall 3")
  </read_first>
  <behavior>
    - Test A (adequate params pass): on a 400-row synthetic cloud with a well-configured ANN, the recall gate returns a measured recall ≥ 0.95 and does NOT throw.
    - Test B (degraded params abort): with a deliberately crippled ANN neighbour source (e.g. a stub that returns mostly-wrong neighbours, or ef pinned tiny with no escalation headroom), the gate escalates then throws `AnnRecallException`; the exception message includes the final measured recall.
    - Test C (cost bound): the exact reference is computed for only the capped sample (≈0.1%, cap 10k) against the full reference — assert the helper is called with a sample size == min(10_000, max(1, round(n*0.001))) (test via a small n where the formula is checkable).
    - Test D (recall formula): meanRecall over identical exact==approx neighbour sets == 1.0; over disjoint sets == 0.0.
  </behavior>
  <action>
    1. Create `qupath.ext.celltune.model.AnnRecallException extends RuntimeException` (unchecked; single `String message` constructor; brief javadoc: thrown only when ANN recall stays below the gate threshold after the ef-escalation cap, so NO Cluster labels are written — the phase's correctness gate per D-08). This is the first custom exception in the model package (no existing analog — see PATTERNS "No Analog Found").

    2. In `LeidenModel`, add package-private static helpers (pure, testable):
       - `static int[] exactNeighborsForQuery(double[][] reference, int queryIdx, int k)` OR `static int[][] exactNeighborsForQueries(double[][] reference, int[] sampleIdx, int k)` — returns neighbour INDICES only (no vote/label) for the sampled query rows against the FULL reference, O(sample × n). ADAPT `nearestReferenceIndices` (do NOT call `featureKnn` — that is O(n²), Pitfall 3). Reuse the existing bounded-heap `nearestForRow`/`worse`/`siftUp`/`siftDown` machinery — no boxing.
       - `static double meanRecall(int[][] exact, int[][] approx)` — mean over rows of |exact_i ∩ approx_i| / |exact_i|.
       - `static double runRecallGate(double[][] rows, int k, int[][] annNeighbors, long seed, IntUnaryOperator/functional escalate)` OR an inline gate inside `clusterViaAnn` (Task 2): sample size = `Math.min(10_000, Math.max(1, (int) Math.round(n * 0.001)))`, seeded via `CohortClusterModel.sampleIndices`-style selection (or a local `Random(seed)`); compute exact neighbours for the sample; compute meanRecall vs the ANN neighbours for the same rows; if < 0.95, raise ef (×2 via the index's `setEf`, re-query the sample) up to a hidden `MAX_ESCALATIONS` cap (e.g. 4); if still < 0.95 → `throw new AnnRecallException(...)`. Return the measured (passing) recall so the caller can surface it to the status line (D-09).
       Constants (`RECALL_THRESHOLD=0.95`, `RECALL_SAMPLE_FRACTION=0.001`, `RECALL_SAMPLE_CAP=10_000`, `MAX_ESCALATIONS`, default `ef`) are hidden defaults (D-09) — private static finals with comments.

    3. Add tests A–D to `LeidenModelTest` (add `import static org.junit.jupiter.api.Assertions.assertThrows;`). For Test B use a small local stub/degraded neighbour array so no real ANN wiring is needed to prove the abort path.
  </action>
  <acceptance_criteria>
    - `src/main/java/qupath/ext/celltune/model/AnnRecallException.java` exists; `grep -c 'extends RuntimeException' src/main/java/qupath/ext/celltune/model/AnnRecallException.java` == 1
    - `grep -E 'meanRecall|exactNeighborsForQ' src/main/java/qupath/ext/celltune/model/LeidenModel.java` matches
    - `grep -c 'featureKnn' src/main/java/qupath/ext/celltune/model/LeidenModel.java` shows featureKnn is NOT called inside the recall-gate helper (recall uses the capped query-vs-reference helper, not all-pairs) — reviewer check; the gate helper body must reference the sample-vs-reference helper
    - `.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"` exits 0 (A–D pass, including the assertThrows abort test)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"</automated>
  </verify>
  <done>Recall gate measures on a capped subsample, escalates ef, and throws AnnRecallException (writing nothing) when recall stays < 0.95; adequate params return a passing recall value. All bounded to O(sample × n).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: clusterViaAnn — route Leiden through HNSW + recall gate</name>
  <files>src/main/java/qupath/ext/celltune/model/LeidenModel.java, src/test/java/qupath/ext/celltune/model/LeidenModelTest.java</files>
  <read_first>
    - src/main/java/qupath/ext/celltune/model/LeidenModel.java (lines 195-247 `cluster` — the entry to mirror; lines 249-297 `buildJaccardWeightedNetwork` — unchanged downstream consumer; lines 218-228 association-strength normalization — do NOT alter)
    - src/main/java/qupath/ext/celltune/model/HnswKnnIndex.java (Plan 01 output — the `knn` API)
    - src/test/java/qupath/ext/celltune/model/LeidenModelTest.java (lines 26-40 brute-force-vs-algorithm test shape; lines 87-113 recovery-by-purity; lines 285-293 `purity` helper style for a new ARI helper)
    - .planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md ("New exception type needed" — new exception, no existing analog, ~L147-148; "Small query set vs large reference set, brute force, parallel" reuse-don't-rewrite pattern, ~L98-117)
  </read_first>
  <behavior>
    - Test A (HNSW-vs-exact ARI): `cluster(rows,...)` (exact) and `clusterViaAnn(rows,...)` on the same synthetic multi-blob cloud produce labelings whose Adjusted Rand Index ≥ tolerance (e.g. ≥ 0.85).
    - Test B (single-image large synthetic completes): `clusterViaAnn` on a larger synthetic cloud (e.g. 2000 rows) returns `labels.length == rows.length` and `nClusters >= 2` without throwing (recall gate passes with default params).
    - Test C (gate abort surfaces): forcing degraded ANN params via the recall-gate wiring throws `AnnRecallException` and yields no LeidenResult.
    - Test D (degenerate): n==0 / n==1 handled exactly as `cluster` (no throw, no gate).
  </behavior>
  <action>
    Add `public static LeidenResult clusterViaAnn(double[][] rows, int k, double resolution, int randomStarts, long seed, boolean reproducible)` (and, if convenient, an overload that also returns/records the measured recall for the status line). Body mirrors `cluster` (lines 209-247) with ONE change to the neighbour source:
      1. Guard n==0/n==1 exactly like `cluster`.
      2. Build ANN neighbours: `int[][] neighbors = HnswKnnIndex.knn(rows, k, seed, reproducible);`
      3. Run the recall gate from Task 1 against exact `featureKnn` on the capped sample; on failure it throws `AnnRecallException` (caller writes nothing). On pass, keep the measured recall for the caller.
      4. Feed `neighbors` into the UNCHANGED `buildJaccardWeightedNetwork(n, neighbors)` → `createNormalizedNetworkUsingAssociationStrength()` → `LeidenAlgorithm.findClustering` pipeline (do NOT re-derive the association-strength normalization — RESEARCH "Code Examples"). Seed the CWTS `Random` from `seed` exactly as `cluster` does.
    Preserve `featureKnn`, `cluster`, and `transferLabels` unchanged (SPEC: transfer + its Phase 14 tests must remain present and passing). Add a small self-contained `static double adjustedRandIndex(int[] a, int[] b)` test helper (no existing analog — follow the `purity` helper style at lines 285-293) either in the test file or as a package-private helper.
    Add tests A–D.
  </action>
  <acceptance_criteria>
    - `grep -c 'clusterViaAnn' src/main/java/qupath/ext/celltune/model/LeidenModel.java` >= 1
    - `grep -c 'createNormalizedNetworkUsingAssociationStrength' src/main/java/qupath/ext/celltune/model/LeidenModel.java` >= 1 (normalization preserved)
    - `grep -c 'public static int\[\] transferLabels' src/main/java/qupath/ext/celltune/model/LeidenModel.java` == 1 (transfer retained)
    - `.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"` exits 0 (ARI, large-synthetic, abort, degenerate all pass; Phase 14 tests still green)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"</automated>
  </verify>
  <done>clusterViaAnn builds Leiden from an HNSW graph, gated by recall, agreeing with exact Leiden by ARI; transferLabels and all Phase 14 tests remain present and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approximate graph → clustering result | An under-recall ANN graph could silently produce wrong communities; the recall gate is the control at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering (integrity of result) | ANN graph with insufficient recall yielding a wrong partition | mitigate | Recall gate measures vs exact featureKnn on a capped subsample; escalates ef; aborts via AnnRecallException (no labels) if < 0.95 after the cap (D-07/D-08). Enforced in `clusterViaAnn`. |
| T-15-04 | Denial of Service | Recall gate accidentally reintroducing O(n²) exact cost | mitigate | Exact reference computed only for the ≤10k capped sample against the full reference (O(sample×n)), never all-pairs featureKnn (Pitfall 3). Verified by Task 1 Test C. |
</threat_model>

<verification>
- `.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"` green (recall pass/escalate/abort, HNSW-vs-exact ARI, retained transfer tests)
- association-strength normalization and transferLabels unchanged (grep gates)
</verification>

<success_criteria>
LEI-07 met for the pure-model layer: HNSW-routed Leiden with an enforced ≥95% recall gate that aborts (writing nothing) on failure, agreeing with exact Leiden by ARI; transfer path intact.
</success_criteria>

<output>
After completion, create `.planning/phases/15-all-cells-leiden-clustering/15-02-SUMMARY.md`
</output>
