---
phase: 15-all-cells-leiden-clustering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/main/java/qupath/ext/celltune/model/HnswKnnIndex.java
  - src/test/java/qupath/ext/celltune/model/HnswKnnIndexTest.java
autonomous: true
requirements: [LEI-07, LEI-10]
user_setup: []

must_haves:
  truths:
    - "The fat JAR bundles com.github.jelmerk:hnswlib-core so ANN classes load at runtime"
    - "An HNSW index can be built over double[][] z-scored rows and queried for each row's k nearest neighbours (Euclidean)"
    - "With the reproducibility toggle ON, two consecutive index builds over the same rows produce identical neighbour lists (structure-identical, up to nothing — byte-identical adjacency)"
    - "Query-time ef can be raised via setEf without rebuilding the index"
  artifacts:
    - path: "build.gradle.kts"
      provides: "hnswlib-core dependency via implementation + shadow"
      contains: "com.github.jelmerk:hnswlib-core:1.2.1"
    - path: "src/main/java/qupath/ext/celltune/model/HnswKnnIndex.java"
      provides: "Pure ANN kNN graph wrapper (build/query/setEf, seeded-deterministic build)"
      contains: "DOUBLE_EUCLIDEAN_DISTANCE"
    - path: "src/test/java/qupath/ext/celltune/model/HnswKnnIndexTest.java"
      provides: "HNSW-vs-exact recall + determinism tests on synthetic clouds"
  key_links:
    - from: "HnswKnnIndex.knn"
      to: "com.github.jelmerk.hnswlib.core.hnsw.HnswIndex.findNearest"
      via: "per-row parallel query loop"
      pattern: "findNearest"
    - from: "HnswKnnIndex reproducible build"
      to: "seeded Random level assignment"
      via: "single-threaded add + seeded assignLevel"
      pattern: "assignLevel|numThreads"
---

<objective>
Add the jelmerk `hnswlib-core` dependency and create the pure `HnswKnnIndex` wrapper that all Leiden kNN graph construction (single-image and cohort) will route through, replacing brute-force `featureKnn`. This is the foundation layer: it introduces the ANN index and — critically — a seeded, deterministic build path that later satisfies SPEC Acceptance Criterion 6.

Purpose: Resolve D-01 (jelmerk chosen) into working, tested code, and settle the D-13/RESEARCH-Pitfall-1 determinism finding (jelmerk's `assignLevel` uses UNSEEDED `ThreadLocalRandom`) before any consumer depends on it.
Output: `hnswlib-core` bundled in the fat JAR; `HnswKnnIndex.java` (build/query/setEf + seeded build); `HnswKnnIndexTest.java` proving HNSW recall vs exact and build-to-build determinism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-all-cells-leiden-clustering/15-SPEC.md
@.planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md
@.planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md

<interfaces>
<!-- jelmerk hnswlib-core 1.2.x API (package com.github.jelmerk.hnswlib.core, post-JPMS repackage). Use these directly; do NOT follow old-package (com.github.jelmerk.knn) tutorials. Verified in 15-RESEARCH.md "API fit detail". -->

Build:
  HnswIndex<TId, double[], TItem, Double> index =
      HnswIndex.newBuilder(dimensions, DistanceFunctions.DOUBLE_EUCLIDEAN_DISTANCE, maxItemCount)
               .withM(m).withEf(ef).withEfConstruction(efConstruction).build();
  // Item<TId,double[]> requires id(), vector(), dimensions().
Query (no whole-graph call — loop per row):
  List<SearchResult<TItem, Double>> res = index.findNearest(vector, k);  // ascending by distance
Escalate recall (query-time only, no rebuild):
  index.setEf(newEf);
Parallel bulk add (non-deterministic order):
  index.addAll(items, numThreads, listener, interval);
DETERMINISM: HnswIndex.assignLevel(double lambda) draws from ThreadLocalRandom.current() with NO seed hook.
  assignLevel is public and HnswIndex is non-final → subclassable. See task 2.
</interfaces>

<!-- Style analog only (no functional ANN analog exists in-repo): LeidenModel's pure-array, JavaFX/QuPath-free convention. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle hnswlib-core into the build</name>
  <files>build.gradle.kts</files>
  <read_first>
    - build.gradle.kts (the existing CWTS block at lines 42-53 — copy its implementation + shadow pairing exactly)
    - .planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md (build.gradle.kts section, lines 21-49)
  </read_first>
  <action>
    Add, alongside the existing `nl.cwts:networkanalysis:1.3.0` block (implementation + shadow), a new commented block for the ANN library, using the EXACT Maven coordinates from D-01/RESEARCH:

    ```kotlin
    // HNSW — approximate-NN kNN graph construction for Leiden at cohort scale
    // (tens of millions of cells). Pure Java, no native binaries (unlike Smile's
    // OpenBLAS/ARPACK bundling) — see .planning/phases/15-.../15-RESEARCH.md "Standard Stack".
    implementation("com.github.jelmerk:hnswlib-core:1.2.1")
    shadow("com.github.jelmerk:hnswlib-core:1.2.1")
    ```

    Do NOT add `hnswlib-core-jdk17` (its SIMD path needs `--add-modules jdk.incubator.vector` launch flag — rejected in RESEARCH "Supporting"). Do NOT touch the spotbugs/spotless blocks.
  </action>
  <acceptance_criteria>
    - `grep -F 'com.github.jelmerk:hnswlib-core:1.2.1' build.gradle.kts` returns exactly 2 lines (implementation + shadow)
    - `grep -c 'hnswlib-core-jdk17' build.gradle.kts` returns 0
    - `.\gradlew.bat compileJava` exits 0 (dependency resolves from Maven Central)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat compileJava</automated>
  </verify>
  <done>Both dependency lines present; project still compiles with the new dependency on the classpath.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HnswKnnIndex wrapper with seeded-deterministic build</name>
  <files>src/main/java/qupath/ext/celltune/model/HnswKnnIndex.java, src/test/java/qupath/ext/celltune/model/HnswKnnIndexTest.java</files>
  <read_first>
    - src/main/java/qupath/ext/celltune/model/LeidenModel.java (lines 37-70 `featureKnn` — mirror the `IntStream.range(0,n).parallel().forEach` per-row query shape; lines 174-185 `squaredDistance` — Euclidean baseline the ANN must be ordering-equivalent to)
    - .planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md ("API fit detail", "Common Pitfalls: Pitfall 1")
    - .planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md (HnswKnnIndex.java section, lines 151-160)
  </read_first>
  <behavior>
    - Test A (recall vs exact): on a 300-row × 5-D synthetic multi-blob cloud, `HnswKnnIndex.knn(rows, k=10, seed, reproducible=true)` neighbour sets overlap ≥ 95% (mean) with `LeidenModel.featureKnn(rows, 10)`.
    - Test B (self-exclusion + length): every row's neighbour list excludes itself and has length == min(k, n-1).
    - Test C (determinism): two consecutive `knn(rows, k, seed=42, reproducible=true)` calls return byte-identical `int[][]` (assertArrayEquals over all rows).
    - Test D (degenerate): n==0 and n==1 return empty/neutral without throwing.
    - Test E (setEf escalation raises recall): building with a deliberately tiny ef then calling the escalation path (or knn at higher ef) yields recall ≥ its low-ef value (monotonic, no rebuild).
  </behavior>
  <action>
    Create `qupath.ext.celltune.model.HnswKnnIndex` — a pure class (no QuPath/JavaFX types), mirroring `LeidenModel`'s static-array convention. Public surface:

    - `public static int[][] knn(double[][] rows, int k, long seed, boolean reproducible)` — build one HNSW index over `rows` (dimensions = rows[0].length) using `DistanceFunctions.DOUBLE_EUCLIDEAN_DISTANCE`, then query `index.findNearest(rows[i], k+1)` per row in an `IntStream.range(0,n).parallel().forEach(...)` loop (shape identical to `featureKnn`), dropping self from results and keeping the first `min(k, n-1)` by ascending distance. Guard n==0/n==1 exactly as `LeidenModel.cluster` does (return empty rows / single empty). Hidden defaults: M (e.g. 16), efConstruction (e.g. 200), initial ef (e.g. 64) — comment them as tunable hidden defaults per D-09.
    - Determinism (D-13 + RESEARCH Pitfall 1): when `reproducible == true`, build the index with a SEEDED level assignment and single-threaded sequential `add()` in fixed row order. Implement by subclassing `HnswIndex` to override `public int assignLevel(double lambda)` to draw from an instance `java.util.Random(seed)` instead of `ThreadLocalRandom.current()` (assignLevel is public, HnswIndex non-final — verified in RESEARCH). If subclassing proves unworkable in 1.2.1 (constructor/access), FALL BACK to single-threaded ordered `add()` only and add a clear javadoc note that graph topology is "best-effort deterministic"; but the determinism test (Test C) MUST pass — do not ship a flaky reproducible mode. When `reproducible == false`, use `addAll(items, numThreads>1, ...)` for the faster parallel build.
    - `public HnswKnnIndex` instance form (REQUIRED — not optional): expose an instance API `build(...)/queryRow(...)/setEf(int)` wrapping the same underlying index, so a live index can be re-queried at a higher `ef` WITHOUT rebuilding. This is mandated by this plan's must_haves.truths ("Query-time ef can be raised via setEf without rebuilding the index") and Behavior Test E, and Plan 02's recall-gate ef-escalation (D-08) depends on it — the gate calls `setEf` then re-queries the sample rather than rebuilding. Keep the third-party `com.github.jelmerk.*` import surface confined to THIS file so `LeidenModel` stays free of it.

    Add a class javadoc documenting: Euclidean (returns true distance, ordering-equivalent to `squaredDistance` — do NOT compare raw distance values across ANN/exact, only neighbour-set membership), the reproducible-vs-fast build split, and the hidden defaults.

    Write `HnswKnnIndexTest` covering behaviours A–E above, reusing a local `randomCloud`/`fillBlob` helper in the style of `LeidenModelTest` (lines 252-293) and a `meanRecall(int[][] exact, int[][] approx)` helper.
  </action>
  <acceptance_criteria>
    - `src/main/java/qupath/ext/celltune/model/HnswKnnIndex.java` exists and imports `com.github.jelmerk.hnswlib.core.DistanceFunctions`
    - `grep -c 'com.github.jelmerk' src/main/java/qupath/ext/celltune/model/LeidenModel.java` returns 0 (ANN import surface confined to HnswKnnIndex)
    - `grep -E 'assignLevel|numThreads' src/main/java/qupath/ext/celltune/model/HnswKnnIndex.java` matches (determinism handling present)
    - `.\gradlew.bat test --tests "qupath.ext.celltune.model.HnswKnnIndexTest"` exits 0 (all of A–E pass, determinism test green — not flaky)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat test --tests "qupath.ext.celltune.model.HnswKnnIndexTest"</automated>
  </verify>
  <done>HnswKnnIndex builds/queries an HNSW graph with ≥95% recall vs exact on synthetic data, excludes self, is byte-identical across two reproducible builds, and handles degenerate inputs; ANN imports confined to this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user project data → in-JVM ANN index | Marker vectors (user's own QuPath data) loaded into an in-memory HNSW graph; no network, no external/untrusted input |
| new third-party dependency → fat JAR classpath | hnswlib-core enters the shadow JAR alongside CWTS/Smile/XGBoost/LightGBM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | HNSW build at very large n (unbounded index) | accept | Scale guard (soft ceiling) is enforced upstream in Plan 05; this pure layer is bounded by caller-supplied `rows`. Pure Java, no native binary → no OOM-in-native risk. |
| T-15-02 | Tampering | Dependency supply chain (hnswlib-core from Maven Central) | accept | Pinned exact version 1.2.1 (Apache-2.0, no CVEs found per RESEARCH A4); no native binaries reduce attack surface vs the JNI alternatives rejected in D-01. |
</threat_model>

<verification>
- `.\gradlew.bat compileJava` green after dependency add
- `.\gradlew.bat test --tests "qupath.ext.celltune.model.HnswKnnIndexTest"` green (recall + determinism)
- ANN import surface confined to `HnswKnnIndex.java` (grep gate above)
</verification>

<success_criteria>
hnswlib-core bundled; `HnswKnnIndex` provides a tested, reproducible ANN kNN graph builder with ≥95% recall vs exact on synthetic clouds, ready for Plan 02 to route Leiden through.
</success_criteria>

<output>
After completion, create `.planning/phases/15-all-cells-leiden-clustering/15-01-SUMMARY.md`
</output>
