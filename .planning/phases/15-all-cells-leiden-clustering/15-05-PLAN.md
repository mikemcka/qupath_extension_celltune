---
phase: 15-all-cells-leiden-clustering
plan: 05
type: execute
wave: 5
depends_on: [15-04]
files_modified:
  - src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java
  - USER_GUIDE.md
  - CLAUDE.md
autonomous: false
requirements: [LEI-06, LEI-07, LEI-09]
user_setup: []

must_haves:
  truths:
    - "The single-image / interactive-preview Leiden fit routes its kNN graph build through the HNSW ANN index (clusterViaAnn), not brute-force featureKnn (D-06)"
    - "In project scope with Method=Leiden, a radio pair 'Cluster all cells' (default) / 'Transfer from sample' is visible; it is hidden in single-image scope and for k-means"
    - "Choosing 'Cluster all cells' runs the two-pass all-cells driver; 'Transfer from sample' runs the retained Phase 14 transfer path"
    - "A soft-ceiling confirm dialog appears before the batch when pooled cells exceed the configurable 50M threshold"
    - "The run reports per-phase progress (Pooling/Building graph/Running Leiden/Writing) and the measured ANN recall to the status line; a Cancel button interrupts mid-run"
    - "After an all-cells write, the scatter legend/overlay re-syncs to the final all-cells cluster count (== count written to cells), not the preview subsample count"
    - "USER_GUIDE and CLAUDE.md document the all-cells mode and the three scanpy-fidelity gaps (CPM not modularity, SNN/Jaccard not UMAP connectivities, no PCA)"
  artifacts:
    - path: "src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java"
      provides: "single-image ANN routing, cohort-mode radio pair, all-cells write action, soft-ceiling confirm, cancel, per-phase progress, post-run re-sync"
      contains: "writeClusterAllCells"
    - path: "USER_GUIDE.md"
      provides: "all-cells mode docs + fidelity gaps"
      contains: "Cluster all cells"
    - path: "CLAUDE.md"
      provides: "fidelity-gap + packed-UUID/determinism notes"
      contains: "all-cells"
  key_links:
    - from: "single-image interactive Leiden fit"
      to: "LeidenModel.clusterViaAnn"
      via: "HNSW graph build replacing LeidenModel.cluster in the preview fit (D-06)"
      pattern: "clusterViaAnn"
    - from: "cohort-mode radio pair"
      to: "CohortClusterModel.writeClusterAllCells"
      via: "all-cells action dispatch (project scope, Leiden)"
      pattern: "writeClusterAllCells"
    - from: "post-run re-sync"
      to: "activateClusterMapper(viewer, allCellsNClusters)"
      via: "final all-cells cluster count, not fitNClusters"
      pattern: "activateClusterMapper"
    - from: "Cancel button"
      to: "CohortClusterModel.CancellationToken.cancel()"
      via: "mid-run interruption"
      pattern: "cancel"
---

<objective>
Wire the HNSW/all-cells work into the UI: (1) route the single-image / interactive-preview Leiden fit through `clusterViaAnn` (D-06, the single-image half of LEI-07); (2) a project-scope-only radio pair selecting all-cells (default) vs transfer, dispatching to `CohortClusterModel.writeClusterAllCells`; (3) a soft-ceiling confirm (D-10), per-phase progress + measured-recall status (D-09/D-11), a Cancel button (D-12); and (4) post-run legend re-sync to the final all-cells cluster count (LEI-09). Document the all-cells mode and the three scanpy-fidelity gaps.

Purpose: Deliver LEI-09 (preview re-sync), the single-image clause of LEI-07 (HNSW in both scopes), and the user-facing half of LEI-06 (mode selection, batch UX, docs).
Output: extended `ScatterPlotView`; USER_GUIDE §11 + CLAUDE.md updates; a human-verify checkpoint for the GUI behaviours that have no headless harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-all-cells-leiden-clustering/15-SPEC.md
@.planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md
@.planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md
@.planning/phases/15-all-cells-leiden-clustering/15-04-SUMMARY.md

<interfaces>
<!-- From LeidenModel (Plans 02/03): -->
  public static LeidenResult clusterViaAnn(double[][] rows, int k, double resolution, int randomStarts, long seed, boolean reproducible);
  // throws AnnRecallException on recall-gate failure.

<!-- From CohortClusterModel (Plan 04): -->
  public static AllCellsResult writeClusterAllCells(project, images, markers, graphK, resolution,
      randomStarts, seed, reproducible, classFilter, normalizer, openData, openName, token, log, progress);
  // AllCellsResult(int nClusters, double recall, long cellsWritten, List<String> imagesWritten,
  //                List<String> imagesNotWritten, boolean aborted, boolean cancelled);
  public static final class CancellationToken { void cancel(); boolean isCancelled(); }

<!-- From ScatterPlotView (existing, mirror/reuse — verified line numbers): -->
  private enum ClusterMethod { KMEANS, LEIDEN }                          // line 103
  private static final int LEIDEN_GRAPH_K = 15;                          // line 109
  reproducibleCheck (CheckBox), methodCombo (ComboBox<ClusterMethod>)    // lines 181-184, 293
  // interactive Leiden fit currently calls LeidenModel.cluster(active, LEIDEN_GRAPH_K, resolution, randomStarts, seed) // line ~849
  private int fitNClusters;                                              // line 153 (preview subsample count — do NOT use for re-sync)
  private boolean clusterMeasurementStale;                               // line 204
  methodCombo show/hide binding                                          // lines 292-346 — MIRROR for the radio pair
  scope ToggleGroup construction                                        // lines 396-424
  private void applyScopeOverrides()                                     // lines ~1793-1803 — EXTEND for radio visibility
  private void writeClusterMeasurementAcrossProject()                    // lines 1558-1665 — confirm→thread→progress→try/finally shape
  private void activateClusterMapper(QuPathViewer viewer, int nClusters) // lines 1696-1715 — REUSE verbatim for re-sync
  LEIDEN_REPRODUCIBLE_STARTS / LEIDEN_SINGLE_START / LEIDEN_SEED         // lines 110-116
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route single-image Leiden fit through HNSW + add the cohort-mode radio pair</name>
  <files>src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java</files>
  <read_first>
    - src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java (lines 27-28 imports; lines 292-346 methodCombo binding; lines 396-424 scope ToggleGroup; lines 763-975 the Recompute background thread incl. the interactive Leiden fit at ~845-855; lines 1793-1803 applyScopeOverrides)
    - .planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md (ScatterPlotView section, "Method-combo show/hide", "Scope ToggleGroup", lines 285-341)
    - .planning/phases/15-all-cells-leiden-clustering/15-CONTEXT.md (D-06)
  </read_first>
  <action>
    A. Single-image / preview HNSW routing (D-06, LEI-07 single-image clause): in the Recompute background thread, replace the interactive Leiden fit call `LeidenModel.cluster(active, LEIDEN_GRAPH_K, resolution, randomStarts, seed)` (~line 849) with `LeidenModel.clusterViaAnn(active, LEIDEN_GRAPH_K, resolution, randomStarts, seed, reproducible)` so the single-image kNN graph is built through the ANN index (no mode selector — D-06). This same `active` fit also produces the `leidenRef`/`leidenRefLabels` reference used by the retained cohort transfer mode, so both now share the ANN graph build. Handle the rare interactive recall-gate failure gracefully: wrap the fit in a try/catch for `AnnRecallException` and surface it to the status line (e.g. "Leiden preview: ANN recall too low — try more cells / different markers"); do NOT crash the Recompute thread or leave the UI disabled (reset controls in the existing finally block). No k-means change.

    B. Cohort-mode radio pair (D-04/D-05): add a `RadioButton`/`ToggleGroup` pair — `cohortModeAllCells` ("Cluster all cells") default-selected and `cohortModeTransfer` ("Transfer from sample") — placed NEXT TO (not replacing) the existing Method combo; label all-cells as exact/true-scanpy default, transfer as fast/approximate. Bind `managedProperty().bind(visibleProperty())` on both and drive visibility from BOTH: `methodCombo.valueProperty()`'s listener (visible only when `value == LEIDEN`, extend the listener at lines 341-346) AND `applyScopeOverrides()` (visible only when `scope == Scope.PROJECT`, extend the method at lines 1793-1803 — do NOT duplicate its logic). Net visibility = `method == LEIDEN && scope == PROJECT`. Do NOT repurpose `methodCombo` (RESEARCH anti-pattern). Add `import javafx.scene.control.RadioButton;` if absent. k-means cohort path unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c 'clusterViaAnn' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1 (single-image fit routed through ANN)
    - `grep -c 'LeidenModel.cluster(' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` == 0 (old brute-force fit call replaced; no direct cluster( call remains)
    - `grep -Ec 'cohortModeAllCells|Cluster all cells' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1
    - `grep -Ec 'Transfer from sample' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1
    - `.\gradlew.bat compileJava` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat compileJava</automated>
  </verify>
  <done>Single-image/preview Leiden builds its graph via clusterViaAnn (recall-gate failure handled gracefully); a radio pair (all-cells default / transfer) is present, visible only in project scope with Method=Leiden, k-means path untouched.</done>
</task>

<task type="auto">
  <name>Task 2: All-cells write action — confirm, progress, cancel, recall status, re-sync</name>
  <files>src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java</files>
  <read_first>
    - src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java (lines 1558-1665 writeClusterMeasurementAcrossProject; lines 1696-1715 activateClusterMapper; lines 1839-1844 pre-flight cancel pattern)
    - .planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md (confirm→thread→progress→try/finally shape lines 343-419; "Pre-flight cancel is NOT mid-run cancel" lines 421-430)
    - .planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md (D-10/D-11/D-12 batch UX)
  </read_first>
  <action>
    When the cohort write action fires with `scope==PROJECT && method==LEIDEN && cohortModeAllCells selected`, dispatch to a new `writeClusterAllCellsAcrossProject()` modeled on `writeClusterMeasurementAcrossProject` (lines 1558-1665) — same confirm→background-Thread→progress/status→try/catch/finally shape. Additions:
      - Soft ceiling (D-10): a configurable threshold (default 50,000,000; use `PathPrefs.createPersistentPreference` per CLAUDE.md conventions). Estimate pooled cell count cheaply before starting (per RESEARCH Open Question 3 — use a count-only per-image scan if no cheaper QuPath accessor exists; planner discretion). If the estimate exceeds the threshold, show an ADDITIONAL `Dialogs.showConfirmDialog(...)` warning before launching; it warns/confirms, does not hard-block.
      - Create a `CohortClusterModel.CancellationToken token`. Show a `Cancel` button (visible only during the run) whose action calls `token.cancel()`. Wire it into the existing `applying`/`setControlsDisabled` lifecycle so it is hidden/reset in the `finally` block.
      - Call `CohortClusterModel.writeClusterAllCells(...)` passing `LEIDEN_GRAPH_K`, the resolution spinner value, `reproducibleCheck.isSelected() ? LEIDEN_REPRODUCIBLE_STARTS : LEIDEN_SINGLE_START`, `reproducibleCheck.isSelected() ? LEIDEN_SEED : System.nanoTime()`, `reproducibleCheck.isSelected()` (the `reproducible` flag), the token, and `msg -> Platform.runLater(() -> statusLabel.setText(msg))` / `frac -> Platform.runLater(() -> progress.setProgress(frac))`. The driver emits per-phase messages ("Pooling X/N images", "Building kNN graph…", "Running Leiden…", "Writing X/N images"). On success, set the status line to include the measured ANN recall (D-09, e.g. "ANN recall 0.982 — passed").
      - On `AllCellsResult.aborted` (recall gate failure) surface an actionable error via `Dialogs.showErrorMessage`/status; no re-sync, `clusterMeasurementStale` stays true.
      - LEI-09 re-sync: on a non-aborted result, call the REUSED `activateClusterMapper(viewer, result.nClusters())` with the FINAL ALL-CELLS cluster count from `AllCellsResult`, NOT `fitNClusters` (the preview subsample count) — set `clusterMeasurementStale = false`. If cancelled, report `imagesWritten`/`imagesNotWritten` counts in the status line (D-12).
    Keep the existing transfer dispatch (`writeClusterAcrossProjectLeiden`) reachable when `cohortModeTransfer` is selected — do NOT remove it.
  </action>
  <acceptance_criteria>
    - `grep -c 'writeClusterAllCells' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1
    - `grep -Ec 'CancellationToken|token.cancel' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1
    - `grep -Ec 'recall' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1 (recall surfaced to status)
    - `grep -c 'activateClusterMapper' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1 (re-sync reuses it; not a new mapper method)
    - `grep -c 'writeClusterAcrossProjectLeiden' src/main/java/qupath/ext/celltune/ui/ScatterPlotView.java` >= 1 (transfer retained)
    - `.\gradlew.bat compileJava` exits 0 and `.\gradlew.bat test` exits 0 (no regression)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat test</automated>
  </verify>
  <done>The all-cells action confirms over the soft ceiling, runs on a background thread with per-phase progress + recall status and a working Cancel, aborts cleanly on recall failure, and re-syncs the legend to the final all-cells cluster count; transfer dispatch retained.</done>
</task>

<task type="auto">
  <name>Task 3: Document all-cells mode + scanpy-fidelity gaps</name>
  <files>USER_GUIDE.md, CLAUDE.md</files>
  <read_first>
    - USER_GUIDE.md (§11.5 project-wide clustering lines 743-796; §11.6 k-means vs Leiden lines 798-820)
    - CLAUDE.md (line 36 CWTS/Leiden dep note; the model/ and ui/ table rows describing ScatterPlotView/CohortClusterModel)
    - .planning/phases/15-all-cells-leiden-clustering/15-SPEC.md (Acceptance Criteria — docs requirement; Out of scope — the three gaps)
  </read_first>
  <action>
    USER_GUIDE.md §11.5/§11.6: document the new project-scope Leiden cohort modes — "Cluster all cells" (default, true-scanpy `sc.tl.leiden`-style: pool every cell → one HNSW kNN graph → single CWTS Leiden partition → UUID write-back) vs "Transfer from sample" (retained Phase 14 `sc.tl.ingest`-style fast/approximate). Also note that single-image Leiden now builds its graph via the same HNSW ANN index (D-06). Describe: the recall-gate status line ("ANN recall 0.982 — passed") and that a run aborts with no labels if recall can't reach 95%; per-phase progress + Cancel (already-written images kept, summary of written/unwritten); the soft cell-count ceiling confirm; and that the persisted `Cluster` comes from the full all-cells run while the interactive preview stays subsample-based (legend re-syncs after the write).
    Add a short "Fidelity vs stock scanpy" note (SPEC-required) listing the THREE documented gaps: (1) CWTS optimises CPM, not scanpy's default modularity/RBConfiguration; (2) SNN/Jaccard weighting, not UMAP fuzzy-simplicial connectivities; (3) no PCA before neighbours (near-identity for ~20-marker panels). State bit-identical reproduction of an external scanpy run is not promised.
    CLAUDE.md: extend the CWTS/Leiden dependency paragraph (line 36 area) and the ScatterPlotView/CohortClusterModel architecture rows to mention: the hnswlib-core dependency and all-cells two-pass mode; the deliberate packed-(long,long)-UUID keying at cohort scale (do NOT "fix" back to getID().toString()); and the reproducibility caveat (jelmerk assignLevel seeded for reproducible mode; parallel/fast mode is best-effort) + the three fidelity gaps.
  </action>
  <acceptance_criteria>
    - `grep -Ec 'Cluster all cells|all-cells' USER_GUIDE.md` >= 1
    - `grep -Eic 'CPM|modularity' USER_GUIDE.md` >= 1 AND `grep -Eic 'UMAP|connectivit' USER_GUIDE.md` >= 1 AND `grep -Eic 'PCA' USER_GUIDE.md` >= 1 (three gaps documented)
    - `grep -Eic 'hnswlib|all-cells|packed' CLAUDE.md` >= 1
    - `.\gradlew.bat spotlessCheck` exits 0 (confirms the Task 1/2 code changes still pass formatting)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat spotlessCheck</automated>
  </verify>
  <done>USER_GUIDE §11 documents both cohort modes, single-image HNSW routing, the recall gate, batch UX, preview-vs-persisted divergence, and the three fidelity gaps; CLAUDE.md notes the dependency, packed-UUID deviation, and reproducibility caveat.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the all-cells GUI flow</name>
  <what-built>The single-image ANN routing, project-scope cohort-mode radio pair, all-cells write action (soft-ceiling confirm, per-phase progress, Cancel, recall status), and post-run legend re-sync — GUI behaviours with no headless test harness (VALIDATION manual-only rows).</what-built>
  <read_first>
    - .planning/phases/15-all-cells-leiden-clustering/15-VALIDATION.md (Manual-Only Verifications table)
    - CLAUDE.md (Build & test — shadowJar + install-in-QuPath steps)
  </read_first>
  <how-to-verify>
    1. `.\gradlew.bat shadowJar` (must be green — confirms hnswlib-core bundles cleanly with CWTS/Smile/XGBoost/LightGBM). Install `build/libs/qupath-extension-celltune-0.2.0-all.jar` into the QuPath extensions folder (delete any older CellTune JAR first), restart QuPath.
    2. Open a multi-image project; open the Scatter plot dialog; single-image scope, Method = Leiden, Recompute — CONFIRM the preview still fits (now via the HNSW path) and the status line shows no ANN error on a normal image.
    3. Switch to Project scope with Method = Leiden. CONFIRM the radio pair "Cluster all cells" (default) / "Transfer from sample" is visible; switch to single-image scope or k-means and CONFIRM it hides.
    4. With "Cluster all cells" selected, run the cohort write. CONFIRM the status line advances through Pooling → Building kNN graph → Running Leiden → Writing X/N, and reports a measured "ANN recall …" value.
    5. After completion, CONFIRM the scatter legend's cluster count equals the count written to cells and the overlay colours match the re-synced legend.
    6. Start another run and click Cancel mid-write; CONFIRM the summary reports which images were / were not updated and already-written images retain their Cluster measurement.
    7. (Optional) Force a low-recall build to confirm the abort surfaces with no Cluster written.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (preview fit, radio visibility, progress phases, recall status, legend count mismatch, cancel behaviour).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user action → long batch job over the whole project | An accidental oversized run can exhaust memory/time; the UI is where the guard + interrupt live |
| background write thread → open image's live FX hierarchy | Mutating the open image must be marshalled to the FX thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Denial of Service | Accidental 100M+-cell all-cells run | mitigate | Soft-ceiling confirm dialog (D-10, configurable 50M default) before any work begins; Cancel button interrupts mid-run (D-12). |
| T-15-12 | Tampering (data integrity) | Off-thread mutation of the open image's live hierarchy | mitigate | Open-image writes go through CohortClusterModel.applyMeasurement's existing Platform.runLater + CountDownLatch marshalling (reused, Plan 04); background images mutated off-thread only. |
| T-15-13 | Repudiation (observability) | User unsure whether an approximate run is trustworthy | mitigate | Measured ANN recall surfaced to the status line (D-09); abort surfaces an actionable error; written/unwritten summary on cancel. |
</threat_model>

<verification>
- `.\gradlew.bat test` green (no regression from UI wiring)
- `.\gradlew.bat shadowJar` green (hnswlib-core bundles cleanly — phase gate)
- `.\gradlew.bat spotlessCheck` green (formatting)
- Human checkpoint approved (single-image preview via HNSW, radio visibility, progress phases, recall status, legend re-sync count, cancel summary)
</verification>

<success_criteria>
LEI-09 + the single-image clause of LEI-07 + the user-facing half of LEI-06 delivered: single-image Leiden via HNSW, mode radio, guarded/observable/cancellable all-cells batch, legend re-sync to the final cluster count, and complete docs including the three fidelity gaps; full `test` + `shadowJar` green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-all-cells-leiden-clustering/15-05-SUMMARY.md`
</output>
