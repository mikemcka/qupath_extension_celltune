---
phase: 15-all-cells-leiden-clustering
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - src/main/java/qupath/ext/celltune/model/LeidenModel.java
  - src/test/java/qupath/ext/celltune/model/LeidenModelTest.java
autonomous: true
requirements: [LEI-06, LEI-10]
user_setup: []

must_haves:
  truths:
    - "SNN/Jaccard edge weighting scales to tens of millions of nodes via primitive sorted-array merge-intersection (no boxed HashSet<Integer>/<Long>)"
    - "The primitive-array weighting produces byte-identical edge weights to the current boxed implementation on small inputs"
    - "clusterViaAnn assigns every pooled row a label from a single partition (labels.length == rows.length), no reference/query split, recovering known synthetic communities by purity"
    - "Two consecutive reproducible clusterViaAnn runs on the same input yield identical labels up to permutation"
  artifacts:
    - path: "src/main/java/qupath/ext/celltune/model/LeidenModel.java"
      provides: "Primitive-array SNN/Jaccard weighting feeding the unchanged CWTS Network"
      contains: "createNormalizedNetworkUsingAssociationStrength"
    - path: "src/test/java/qupath/ext/celltune/model/LeidenModelTest.java"
      provides: "boxed-vs-primitive equivalence + all-cells recovery + reproducibility-up-to-permutation tests"
  key_links:
    - from: "primitive-array Jaccard weighting"
      to: "CWTS Network(n, true, edges, weights, false, true)"
      via: "same constructor + association-strength normalization contract"
      pattern: "new Network\\("
    - from: "clusterViaAnn reproducible run"
      to: "identical labels up to permutation"
      via: "seeded single-threaded ANN build + seeded CWTS Leiden"
      pattern: "reproducible"
---

<objective>
Rewrite the SNN/Jaccard edge-weighting step from boxed `HashSet<Integer>[]` / `HashSet<Long>` to primitive sorted-`int[]` neighbour arrays with merge-based intersection counting so it scales to ~30M nodes (RESEARCH Pitfall 2), keeping byte-identical weighting semantics. Then prove the all-cells clustering behaviour end-to-end on the pure-model layer: single-partition label assignment, community recovery, and reproducibility up to permutation (SPEC Acceptance Criterion 6).

Purpose: Deliver LEI-06's pure-model core (single partition over all pooled rows) and the scale rewrite the cohort path depends on, with equivalence-to-current tests proving semantics are preserved.
Output: primitive-array weighting in `LeidenModel` feeding the unchanged CWTS `Network`; tests for boxed-vs-primitive equivalence, all-cells recovery, and reproducibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-all-cells-leiden-clustering/15-SPEC.md
@.planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md
@.planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md
@.planning/phases/15-all-cells-leiden-clustering/15-02-SUMMARY.md

<interfaces>
<!-- Existing boxed implementation to replace (LeidenModel.java lines 249-324): -->
  private static Network buildJaccardWeightedNetwork(int n, int[][] neighbors);   // uses HashSet<Integer>[] + HashSet<Long>
  private static Set<Integer>[] buildClosedNeighborSets(int n, int[][] neighbors);
  private static double jaccard(Set<Integer> a, Set<Integer> b);
<!-- MUST remain identical (do not alter): -->
  new Network(n, true, new LargeIntArray[]{from, to}, weights, false, true);
  network.createNormalizedNetworkUsingAssociationStrength();
<!-- Jaccard is over CLOSED neighbour sets (each node includes itself) — PhenoGraph/SNN convention. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Primitive-array SNN/Jaccard rewrite (byte-identical semantics)</name>
  <files>src/main/java/qupath/ext/celltune/model/LeidenModel.java, src/test/java/qupath/ext/celltune/model/LeidenModelTest.java</files>
  <read_first>
    - src/main/java/qupath/ext/celltune/model/LeidenModel.java (lines 249-324 `buildJaccardWeightedNetwork`/`buildClosedNeighborSets`/`jaccard` — the exact target; lines 285-296 the Network constructor comment about sortedEdges=false)
    - .planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md ("Common Pitfalls: Pitfall 2", "Anti-Patterns")
    - .planning/phases/15-all-cells-leiden-clustering/15-PATTERNS.md (LeidenModel primitive-array rewrite target, lines 121-147)
  </read_first>
  <behavior>
    - Test A (equivalence): for several small synthetic neighbour arrays (n = 10..200, varied k), the new primitive-array weighting produces the SAME set of undirected edges with BYTE-IDENTICAL (exact `==`) Jaccard weights as the current boxed `buildJaccardWeightedNetwork`. Jaccard here is a ratio of integer intersection/union counts (no float summation-order sensitivity), so bit-identity is achievable and required — assert exact equality, NOT a tolerance. Prove by exposing both weighting paths and comparing edge lists + weights directly with exact equality (or by comparing the resulting Leiden labels via `cluster`).
    - Test B (closed-set convention preserved): two nodes that are each other's only neighbour get weight 1.0 (not 0/undefined) — the closed-set behaviour from the current javadoc (lines 255-257).
  </behavior>
  <action>
    Replace the boxed weighting with a primitive-array implementation that is semantically byte-identical on the closed-set Jaccard:
    - Build per-node sorted `int[]` CLOSED neighbour arrays (each node's own index inserted, then its ANN neighbours, deduplicated and sorted ascending). This replaces `buildClosedNeighborSets`' `HashSet<Integer>[]`.
    - Compute Jaccard between two closed neighbour arrays via a two-pointer merge-intersection walk (O(k) per pair, no hashing/boxing) → `|A∩B| / |A|+|B|-|A∩B|`. This replaces the `jaccard(Set,Set)` method.
    - Emit each undirected edge (a<b) exactly once. Replace the global `HashSet<Long> seenEdges` dedup: prefer symmetrize-by-construction (emit edge (i,j) only when j appears in i's neighbour list AND i appears in j's list, or when i<j and j is in i's neighbours) so no global boxed set is needed; if full symmetry can't be guaranteed cheaply, use a lightweight open-addressing primitive long-set instead of `HashSet<Long>`. Document the chosen approach.
    - Feed the resulting primitive `from`/`to`/`weights` into the UNCHANGED `new Network(n, true, new LargeIntArray[]{from,to}, weights, false, true)` and keep the caller's `createNormalizedNetworkUsingAssociationStrength()` call intact — the constructor signature and normalization contract MUST NOT change (CLAUDE.md warns sortedEdges=true corrupts CSR adjacency).
    - You MAY keep the old boxed method available (e.g. renamed `buildJaccardWeightedNetworkBoxed`) purely so Test A can assert equivalence, or reconstruct a reference in the test. Do not leave dead code in the production path — the primitive version is what `cluster` and `clusterViaAnn` call.
    - Add a code comment flagging that primitive arrays are a deliberate cohort-scale choice per RESEARCH Pitfall 2, so it is not "simplified" back to HashSets in review.
  </action>
  <acceptance_criteria>
    - `grep -c 'HashSet<Integer>' src/main/java/qupath/ext/celltune/model/LeidenModel.java` == 0 in the production weighting path (boxed reference, if kept, must be clearly test-only/named `*Boxed`)
    - `grep -c 'new Network(' src/main/java/qupath/ext/celltune/model/LeidenModel.java` >= 1 with the `, false, true)` suffix preserved (constructor unchanged)
    - `grep -c 'createNormalizedNetworkUsingAssociationStrength' src/main/java/qupath/ext/celltune/model/LeidenModel.java` >= 1
    - `.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"` exits 0 (equivalence + closed-set tests pass; all prior Leiden tests still green)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"</automated>
  </verify>
  <done>SNN/Jaccard weighting uses primitive sorted arrays + merge intersection, producing byte-identical edge weights to the boxed version on small inputs, feeding the unchanged CWTS Network.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: All-cells recovery + reproducibility-up-to-permutation tests</name>
  <files>src/test/java/qupath/ext/celltune/model/LeidenModelTest.java, src/main/java/qupath/ext/celltune/model/LeidenModel.java</files>
  <read_first>
    - src/test/java/qupath/ext/celltune/model/LeidenModelTest.java (lines 87-113 `clusterRecoversThreeSeparatedBlobsByPurity`; lines 140-152 `clusterSameSeedProducesIdenticalLabels`; helpers lines 252-293)
    - .planning/phases/15-all-cells-leiden-clustering/15-RESEARCH.md ("Common Pitfalls: Pitfall 1" — the determinism warning sign)
    - .planning/phases/15-all-cells-leiden-clustering/15-SPEC.md (Acceptance Criteria 2 and 6)
  </read_first>
  <behavior>
    - Test A (single partition / label count == cell count, LEI-06): build a synthetic multi-image-style pooled cloud (3+ separated blobs, no image concept at this layer), run `clusterViaAnn`, assert `res.labels().length == rows.length` (every row labelled from ONE partition, no reference/query split) and per-blob purity > 0.9.
    - Test B (reproducibility up to permutation, SPEC AC6): two consecutive `clusterViaAnn(rows, k, res, starts, seed=99L, reproducible=true)` runs yield labelings that are identical up to a label permutation — assert via a permutation-invariant check (e.g. Adjusted Rand Index == 1.0, or a bijection between the two labelings). This is the test that goes flaky if Plan 01's seeded-assignLevel determinism was skipped — it MUST be stable, not @Disabled.
    - Test C (transfer still works): a quick assertion that `transferLabels` (Phase 14) still produces valid labels — guards SPEC AC "transferLabels and its Phase 14 tests remain present and passing".
  </behavior>
  <action>
    Add tests A–C to `LeidenModelTest`, reusing `fillBlob`/`randomCloud`/`purity` and the ARI helper from Plan 02. For Test B, use the reproducible path (`reproducible=true`, fixed seed) and assert permutation-invariant identity (ARI == 1.0 across the two runs) — do NOT assert raw `assertArrayEquals` on labels unless the label ids are themselves stable; "up to permutation" is the SPEC guarantee. If the run-to-run identity is flaky, that is a real defect in the Plan 01 determinism path — surface it (fail loudly), do not weaken the assertion. Keep all tests at CI-appropriate synthetic sizes (hundreds of rows), never 30M.
  </action>
  <acceptance_criteria>
    - `grep -Ec 'labels\(\)\.length' src/test/java/qupath/ext/celltune/model/LeidenModelTest.java` >= 1 (label-count==cell-count assertion present)
    - `grep -Eic 'reproduc|permutation|adjustedRandIndex' src/test/java/qupath/ext/celltune/model/LeidenModelTest.java` >= 1
    - `.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"` exits 0 (A–C pass; reproducibility test stable across runs)
    - `.\gradlew.bat test` exits 0 (full suite — no regression in retained transfer path or other model tests)
  </acceptance_criteria>
  <verify>
    <automated>.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"</automated>
  </verify>
  <done>All-cells clustering assigns every row a label from one partition and recovers synthetic communities by purity; reproducible runs are identical up to permutation; transfer path validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| large pooled matrix → weighting step | The edge-weighting step is where cohort-scale memory blows up if boxed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Denial of Service | Boxed HashSet weighting exhausting heap at ~30M nodes | mitigate | Primitive sorted-int[] + merge-intersection rewrite removes tens-of-GB boxing overhead (Pitfall 2). Correctness preserved via boxed-vs-primitive equivalence test. |
| T-15-06 | Tampering (integrity of result) | Rewrite silently changing edge weights / cluster semantics | mitigate | Byte-identical equivalence test (Task 1 A) vs the current boxed implementation on small inputs; CWTS Network constructor + association-strength normalization unchanged. |
</threat_model>

<verification>
- `.\gradlew.bat test --tests "qupath.ext.celltune.model.LeidenModelTest"` green (equivalence, recovery, reproducibility)
- `.\gradlew.bat test` green (no regression)
- Network constructor `, false, true)` and association-strength normalization preserved (grep gates)
</verification>

<success_criteria>
LEI-06 pure-model core delivered: scalable primitive-array weighting with proven-equivalent semantics; all-cells single-partition recovery and reproducibility-up-to-permutation tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-all-cells-leiden-clustering/15-03-SUMMARY.md`
</output>
