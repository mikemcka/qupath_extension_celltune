---
phase: 03-composite-rule-contract-and-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/qupath/ext/celltune/classifier/CompositeClassificationRule.java
  - src/main/java/qupath/ext/celltune/io/ProjectStateManager.java
autonomous: true
requirements:
  - COMP-01
  - COMP-04
must_haves:
  truths:
    - "Composite rules are represented as named marker+polarity conditions rather than marker-only lists"
    - "Named composite rules persist to project storage and can be loaded after restart"
    - "Rule serialization rejects malformed/unsafe marker names before persistence"
  artifacts:
    - path: "src/main/java/qupath/ext/celltune/classifier/CompositeClassificationRule.java"
      provides: "Rule contract for named marker-polarity definitions"
      exports: ["CompositeClassificationRule"]
    - path: "src/main/java/qupath/ext/celltune/io/ProjectStateManager.java"
      provides: "composite-rules.json persistence API"
      contains: "saveCompositeRules"
  key_links:
    - from: "CompositeClassificationRule"
      to: "ProjectStateManager.saveCompositeRules"
      via: "JSON contract persists named rule + conditions"
      pattern: "composite-rules.json"
    - from: "ProjectStateManager.loadCompositeRules"
      to: "CompositeClassificationDialog"
      via: "returns reusable named rules for UI selection"
      pattern: "loadCompositeRules"
---

<objective>
Build the Phase 3 foundation for explicit composite rules.

Purpose: Close the contract and persistence gaps from milestone audit by introducing a first-class rule model (name + marker polarities) and project-level named-rule storage.
Output: New `CompositeClassificationRule` contract and new `ProjectStateManager` APIs backed by `<project>/celltune/composite-rules.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/03-composite-rule-contract-and-persistence/03-GAP-CONTEXT.md

<interfaces>
From src/main/java/qupath/ext/celltune/io/ProjectStateManager.java:
```java
public class ProjectStateManager {
    private static final Gson GSON = new GsonBuilder().setPrettyPrinting().create();
    public static Path getCellTuneDir(Project<?> project) throws IOException;
    public static SavedState loadBinaryState(Project<?> project, String sanitizedMarkerName) throws IOException;
    public static void saveCompositeConfig(Project<?> project, List<String> selectedMarkers) throws IOException;
    public static List<String> loadCompositeConfig(Project<?> project) throws IOException;
}
```

From src/main/java/qupath/ext/celltune/io/BinaryClassifierRegistry.java:
```java
public class BinaryClassifierRegistry {
    public static String sanitizeMarkerName(String markerName);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit composite rule contract with parse/format validation</name>
  <files>src/main/java/qupath/ext/celltune/classifier/CompositeClassificationRule.java</files>
  <action>Create `CompositeClassificationRule` as an immutable value object containing: rule name, ordered list of marker conditions, and a canonical expression form (example: `CD4+:CD3+:CD20-`). Add a nested condition type with marker name and polarity enum; include strict validation so duplicate markers, empty names, malformed polarity tokens, or unsafe marker text are rejected early. Provide static parse and format helpers to convert between expression text and condition list. Keep marker validation aligned with `BinaryClassifierRegistry.sanitizeMarkerName` rules to avoid path or key mismatches later.</action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>`CompositeClassificationRule` can represent marker-polarity pairs and serialize/deserialize deterministic expressions used by UI and persistence code.</done>
</task>

<task type="auto">
  <name>Task 2: Persist named composite rules in ProjectStateManager</name>
  <files>src/main/java/qupath/ext/celltune/io/ProjectStateManager.java</files>
  <action>Add `saveCompositeRules(Project<?>, List&lt;CompositeClassificationRule&gt;)` and `loadCompositeRules(Project&lt;?&gt;)` that read/write `<project>/celltune/composite-rules.json`. Use a versioned JSON envelope (`version`, `rules`) and preserve insertion order so UI presentation is stable. Validate on load: skip malformed rules with log warnings instead of failing the whole load. Keep existing `saveCompositeConfig/loadCompositeConfig` behavior for backward compatibility; do not remove it in this plan.</action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>Named composite rules are saved and loaded from `composite-rules.json` with resilient parsing and backward compatibility preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> rule parser | Rule expression/name fields are untrusted and must be validated |
| project JSON -> runtime objects | `composite-rules.json` can be malformed or tampered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | rule JSON load path | mitigate | Validate schema/version and reject invalid marker/polarity entries while continuing with valid rules |
| T-03-01-02 | D | rule parser | mitigate | Enforce max condition count and input length checks before parsing expression text |
| T-03-01-03 | E | marker name handling | mitigate | Normalize and validate marker names through registry-compatible sanitization rules |
</threat_model>

<verification>
- `./gradlew compileJava`
- Confirm `composite-rules.json` schema includes version and named rule list structure.
</verification>

<success_criteria>
- `CompositeClassificationRule` exists with name + marker polarity conditions.
- Rules can be parsed from and formatted to canonical expression text.
- `ProjectStateManager` persists named rules in `<project>/celltune/composite-rules.json`.
- Loading rules is resilient to malformed records (skip + warn behavior).
</success_criteria>

<output>
After completion, create `.planning/phases/03-composite-rule-contract-and-persistence/03-01-SUMMARY.md`
</output>
